# ARJUNA TMS — Frontend environment
# Base URL of the backend API. When unset, the app falls back to bundled mock data.
VITE_API_BASE_URL=http://localhost:5000/api
# Set to "true" to force mock data even when an API URL is present.
VITE_USE_MOCKS=false